SQL Server Backup Manager

Инструмент с графическим интерфейсом (GUI) для управления резервными копиями баз данных MS SQL Server. Написан на Python с использованием PySide6.

Возможности

Менеджер подключений: Сохранение истории подключений (Сервер/Логин) для быстрого доступа.

Массовый бэкап: Возможность выбрать несколько баз данных галочками и сделать бэкап в один клик.

Сжатие данных: Поддержка нативного SQL сжатия (WITH COMPRESSION), что уменьшает размер файлов в разы.

Восстановление (Restore): Удобный интерфейс для восстановления базы из .bak файла с автоматическим отключением активных пользователей.

Планировщик: Встроенный таймер для запуска бэкапа в указанное время.

Многопоточность: Интерфейс не зависает во время выполнения тяжелых операций.

Установка

Установите Python (если не установлен).

Установите драйвер ODBC для SQL Server (обычно он уже есть в Windows).

Установите необходимые библиотеки:

pip install -r requirements.txt

Конфигурация

Создайте файл `.env` в корне проекта (можно скопировать из `config.py` и изменить значения):

```
DEFAULT_BACKUP_PATH=
SETTINGS_FILE=connection_history.json
ODBC_DRIVER=ODBC Driver 17 for SQL Server
DEFAULT_USER=
SCHEDULER_CHECK_INTERVAL=30000
```

Все настройки можно переопределить через переменные окружения.

Запуск

Запустите файл main.py:

python main.py


Требования

Windows (рекомендуется) или Linux с установленным msodbcsql.

Доступ к MS SQL Server.

Права пользователя SQL Server должны позволять выполнять BACKUP DATABASE и RESTORE DATABASE.

Об авторе

Разработчик: Tigran

GitHub: https://github.com/TigranSo

Telegram: @tigran_so

Если программа была полезна, поставьте ⭐️ звездочку репозиторию!
